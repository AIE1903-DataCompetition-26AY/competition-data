# 无线网络流量预测竞赛——数据说明

## 1. 数据集概述

本赛题提供来自 **217 个匿名化无线网络小区（Cell）** 的 **上行** 网络流量时间序列。数据已按 5 分钟窗口对窗口内流量 **求和** 后提供，采样间隔为 **5 分钟**，连续覆盖 **7 天 + 5 分钟**，每个小区均有 **2,017** 个时间点。

每个无线网络小区对应一条独立的流量时间序列。完整一天包含：

\[
24 \times 60 \div 5 = 288
\]

个时间点。因此，每个小区在本数据集中的观测长度为：

\[
288 \times 7 + 1 = 2{,}017
\]

时间范围为 `2024-11-30 23:00:00` 至 `2024-12-07 23:00:00`（含两端）。两端相隔整 7 天，再计入闭合端点，因此每个小区为 2,017 个时间点，总时长为 7 天加 5 分钟。

整个数据集共包含 **217 条** 无线网络流量时间序列。全部小区在全部时间点上均有记录，总观测数为：

\[
217 \times 2{,}017 = 437{,}689
\]

数据用于刻画无线网络流量的日内周期、高峰与低谷，以及不同小区之间的规模差异，服务于无线网络流量预测与网络资源管理。

---

## 2. 数据基本信息

| 属性 | 说明 |
|---|---|
| 无线网络小区数量 | 217 |
| 时间点数（每个小区） | 2,017 |
| 数据持续时间 | 7 天 + 5 分钟 |
| 时间粒度 | 5 分钟 |
| 每小时采样点数 | 12 |
| 每天采样点数 | 288 |
| 总观测数 | 437,689 |
| 缺失记录数 | 0 |
| 数据起始时间 | `2024-11-30 23:00:00`（周六） |
| 数据结束时间 | `2024-12-07 23:00:00`（周六） |
| 时区 | 按采集记录时间；未另行指定时区 |
| 原始矩阵形状 | `(T=2017, N=217)`，`float32`，取值实际为整数 |

每条记录表示对应小区在一个 **5 分钟统计窗口** 内的上行流量之和。

时间戳 `timestamp` 表示 **该统计窗口的开始时刻**。时间 `t` 对应的统计区间为：

```text
[t, t + 5 min)
```

例如，`2024-11-30 23:00:00` 对应区间 `[2024-11-30 23:00:00, 2024-11-30 23:05:00)`。

时间戳为真实采集日历，覆盖 2024 年 11 月 30 日周六 23:00 至 12 月 7 日周六 23:00。参赛者可以使用星期、是否周末等由时间戳导出的特征；由于每个星期几只出现一次，星期效应不宜过拟合。

---

## 3. 网络流量定义

目标变量 `traffic` 表示对应无线网络小区在指定 5 分钟窗口内的 **上行流量求和计数**。

| 属性 | 说明 |
|---|---|
| 指标名称 | 上行网络流量（Uplink Traffic） |
| 统计窗口 | 5 分钟 |
| 指标含义 | 该小区在窗口内的上行流量总和 |
| 聚合方式 | 对 5 分钟窗口内的上行流量 **求和** |
| 数据单位 | 脱敏后的原始计数单位（非公开的 Byte / KB / Mbps 换算） |
| 上下行口径 | **上行流量** |
| 数值变换 | **未**做归一化、标准化或对数变换；竞赛发布值与 `13.npz` 中的 `x` 一致 |
| 取值类型 | 非负整数；全量数据中最小值为 1，最大值为 28,952,520 |

实测分布（全部 437,689 条记录）：

| 统计量 | 数值 |
|---|---:|
| 最小值 | 1 |
| 1% 分位 | 8,914 |
| 25% 分位 | 174,401 |
| 中位数 | 470,487 |
| 均值 | 915,158 |
| 75% 分位 | 1,076,228 |
| 99% 分位 | 7,327,659 |
| 最大值 | 28,952,520 |
| `traffic ≥ 10^6` 的比例 | 27.09% |

小区之间的平均流量差异很大：小区均值的中位数约为 \(5.80\times 10^5\)，最低约 \(3.96\times 10^4\)，最高约 \(7.92\times 10^6\)。

---

## 4. 数据实体

数据包含 **217 个无线网络小区**，与原始矩阵列顺序一一对应。

每个小区使用唯一匿名标识 `cell_id`：

| cell_id | 原始列下标 | 含义 |
|---|---:|---|
| `Cell_001` | 0 | 第 1 个匿名无线网络小区 |
| `Cell_002` | 1 | 第 2 个匿名无线网络小区 |
| `...` | ... | ... |
| `Cell_217` | 216 | 第 217 个匿名无线网络小区 |

为保护实际通信网络信息，小区真实名称、物理位置、基站标识以及其他可能识别实际网络位置的信息 **均已匿名化，不向参赛者公开**。

不同 `cell_id` 始终对应同一个无线网络小区，因此可以使用同一小区的历史时刻建模。

> 说明：内部研究曾从 217 个小区中剔除 9 个存在明显水平跳变的小区，再抽样 100 个小区做实验（`13_n100_clean`）。**本赛题使用全量 217 个小区，不剔除。**

---

## 5. 数据字段

数据采用 **宽表**：每一行是一个时间戳，每一列是一个小区。

| 字段 | 类型 | 含义 |
|---|---|---|
| `timestamp` | datetime | 当前 5 分钟统计窗口的开始时刻，格式 `YYYY-MM-DD HH:MM:SS`；按时间升序排列 |
| `Cell_001` … `Cell_217` | integer | 对应小区在该时间窗口内的上行流量求和计数 |

数据示例：

```csv
timestamp,Cell_001,Cell_002,...,Cell_217
2024-11-30 23:00:00,679910,184242,...,<value>
2024-11-30 23:05:00,588123,<value>,...,<value>
2024-11-30 23:10:00,738886,<value>,...,<value>
```

某一列从上到下即为该小区的完整流量时间序列；某一行从左到右即为同一时刻全部小区的流量截面。

---

## 6. 数据文件

向参赛者提供的文件如下。

### 6.1 `train.csv`

训练数据，包含可用于建模的历史流量。列为 `timestamp` 与 `Cell_001`–`Cell_217`。

| 项目 | 数值 |
|---|---:|
| 时间范围 | `2024-11-30 23:00:00` – `2024-12-06 22:55:00` |
| 行数（时间点数） | 1,728（整 6 天） |
| 小区列数 | 217 |
| 流量观测数 | 374,976 |

### 6.2 `test.csv`

测试数据给出需要预测的时间戳，以及与训练集相同的小区列。小区列留空，**不含**真实流量。

| 项目 | 数值 |
|---|---:|
| 时间范围 | `2024-12-06 23:00:00` – `2024-12-07 23:00:00` |
| 行数（时间点数） | 289（1 天 + 5 分钟） |
| 小区列数 | 217 |
| 待预测单元格数 | 62,713 |

测试段真实流量由竞赛平台用于评分，不向参赛者公开。

### 6.3 `sample_submission.csv`

提交示例，格式与评分文件一致：第一列为 `timestamp`，其后为 `Cell_001`–`Cell_217` 的预测值。

```csv
timestamp,Cell_001,Cell_002,...,Cell_217
2024-12-06 23:00:00,<prediction>,<prediction>,...,<prediction>
2024-12-06 23:05:00,<prediction>,<prediction>,...,<prediction>
...
```

- 必须覆盖 `test.csv` 中每一个时间戳，以及全部 217 个小区列，不得缺失、不得新增。
- 建议行顺序、列顺序与 `test.csv` 保持一致。
- 各小区列应为数值；预测值可以是浮点数。
- 示例文件中的数值是 **季节朴素基线**（用训练集最后一天同一时刻的流量填入），仅用于演示格式，不是标准答案。

---

## 7. 训练集与测试集划分

本赛题按 **时间先后** 划分，不对小区或时间点做随机抽样。

### 训练集时间范围

```text
2024-11-30 23:00:00 — 2024-12-06 22:55:00
（第 0 至第 1727 个窗口，共 1,728 点 / 6 天）
```

### 测试集时间范围

```text
2024-12-06 23:00:00 — 2024-12-07 23:00:00
（第 1728 至第 2016 个窗口，共 289 点 / 24 小时 5 分钟）
```

模型只能使用测试时刻之前已经提供的信息。测试段标签在评测前不可见，因此这是一次 **多序列、多步直推** 任务：对全部 217 个小区，预测未来连续 289 个 5 分钟点。

| 预测点数 | 对应预测时长 |
|---:|---|
| 289 | 24 小时 5 分钟 |

允许使用：

- 单个小区自身的历史流量；
- 时间周期与日内位置等由时间戳导出的特征；
- 不同小区之间的相关性；
- 仅由公开训练数据构造的其他特征。

不允许使用测试段真实 `traffic`，也不允许使用未公开的外部运营数据。

---

## 8. 数据完整性与异常值说明

### 8.1 缺失值

全量 217 个小区在 2,017 个时间点上 **均有记录**，不存在 `NaN`、无穷值或空缺时间点。时间轴为严格均匀的 5 分钟网格，无需补齐。

### 8.2 零值

本赛题发布的全量数据中 **不存在** `traffic = 0`。全部观测满足 `traffic ≥ 1`。

若参赛者在自己的中间处理中得到 0，不应把它解释为源数据缺失。

### 8.3 异常值与水平跳变

流量存在短时高峰，最大值约为 \(2.90\times 10^7\)。这类突发高流量可能来自真实业务变化，不应简单当作录入错误。

另外，少数小区在日与日之间存在明显的水平跳变（对数日均值的最大日变化可超过 1.0）。这些小区仍保留在全量数据中。参赛者可自行决定是否做异常检测、分段或稳健处理。

### 8.4 日内周期

按采集时钟统计，全量数据的平均流量在凌晨约 4 时最低、约 21 时最高，呈现典型的日内高峰—低谷形态。不同小区的周期强度和相位并不完全一致。

---

## 9. 数据匿名化与隐私保护

本数据集已经过匿名化处理：

- 不包含用户身份信息；
- 不提供小区真实地理位置、基站名称或其他可识别网络设施的信息；
- `cell_id` 只用于区分小区，没有地理或业务含义；
- 时间戳保留真实采集日历，但不提供小区位置，不能据此识别具体网络设施。

流量数值保持源数据中的原始计数，**没有**额外做归一化或缩放。数值本身不能直接换算为真实物理流量单位。

---

## 10. 数据特点

本数据集属于 **高频、多序列无线网络流量时间序列**。

### 10.1 高时间分辨率

时间粒度为 5 分钟，每个小区每天 288 个点，可以反映业务随时间的细变化。

### 10.2 多时间序列

同时覆盖 217 个小区。不同小区的流量规模、周期和波动差异明显。

### 10.3 日内周期性

流量随时钟呈现凌晨低谷、晚间高峰。主周期长度为 288 个时间点（1 天）。

### 10.4 小区间异质性

小区均值可相差两个数量级以上，因此同一套超参数未必适合所有小区。

### 10.5 小区间潜在相关性

不同小区在相同时段可能同步变化，也可以联合建模。

### 10.6 历史较短

每个小区只有约 7 天历史。长周期（如星期效应）无法从数据中可靠估计，模型应主要依赖日内周期、近期水平和跨小区信息。

---

## 11. 使用说明

参赛者提交的预测文件应满足：

1. 第一列为 `timestamp`，其后列为 `Cell_001` 至 `Cell_217`，列名与顺序与 `test.csv` 一致；
2. 行集合与 `test.csv` 中的 `timestamp` 一一对应，不得缺失、不得新增；
3. 每一个小区列的每一个时间戳都必须给出预测值；
4. 预测值为数值类型。

建议预测值为非负数。评测时若出现非数值或缺失单元格，该次提交视为无效。

---

## 12. 评测指标

本赛题设置 **5 项官方指标**，**每一项单独排名、各评一名冠军**。参赛者只需提交一份预测文件，平台对同一份结果分别计算全部指标。

记测试集全部单元格为 \(\{(y_i,\hat{y}_i)\}_{i=1}^{n}\)，\(n=62{,}713\)。Masked MAPE 只在真实值达到阈值的单元格上计算：

\[
\mathrm{MaskedMAPE}(\tau)=\frac{100}{\lvert S_\tau\rvert}\sum_{i\in S_\tau}\frac{\lvert \hat{y}_i-y_i\rvert}{y_i},\qquad S_\tau=\{\,i:y_i\ge \tau\,\}.
\]

| 赛道 | 指标 | 定义 | 计算范围 | 说明 |
|---|---|---|---|---|
| 1 | **MAE** | \(\frac{1}{n}\sum_i \lvert \hat{y}_i-y_i\rvert\) | 全部测试单元格 | 与原始计数同量纲 |
| 2 | **RMSE** | \(\sqrt{\frac{1}{n}\sum_i (\hat{y}_i-y_i)^2}\) | 全部测试单元格 | 更放大高峰误差 |
| 3 | **MAPE** | \(\frac{100}{n}\sum_i \lvert \hat{y}_i-y_i\rvert/y_i\) | 全部测试单元格 | 本数据 \(y_i\ge 1\)，可直接计算 |
| 4 | **Masked MAPE @ 3e5** | \(\mathrm{MaskedMAPE}(3\times 10^5)\) | \(y_i\ge 3\times 10^5\)（测试集约 39,144 个，占 62.4%） | 关注中高流量 |
| 5 | **Masked MAPE @ 1e6** | \(\mathrm{MaskedMAPE}(10^6)\) | \(y_i\ge 10^6\)（测试集约 17,132 个，占 27.3%） | 关注高流量 |

五项指标均为 **越小越好**。每个赛道独立出冠军：该赛道指标最低者获胜；同一赛道数值完全相同，以有效提交时间较早者获胜。不同赛道的冠军可以是同一支队伍，也可以是不同队伍。

Masked MAPE 的掩码只由 **真实值** \(y_i\) 决定，与预测值无关。阈值之外的单元格不进入该赛道的平均。

---

## 13. 数据规模汇总

| 指标 | 数值 |
|---|---:|
| 无线网络小区数量 | 217 |
| 时间粒度 | 5 分钟 |
| 每小时采样点数 | 12 |
| 每天采样点数 | 288 |
| 数据持续时间 | 7 天 + 5 分钟 |
| 每个小区观测点数 | 2,017 |
| 时间序列数量 | 217 |
| 总观测数 | 437,689 |
| 训练表规模 | 1,728 行 × 217 小区 |
| 测试表规模 | 289 行 × 217 小区 |
| 训练流量观测数 | 374,976 |
| 测试待预测单元格数 | 62,713 |
| 预测步数 | 289 |

以上数字均由全量矩阵 `x.shape = (2017, 217)` 直接计算，不是理论估算。

---

## 14. 源数据核对说明

| 初稿写法 | 全量数据实测 | 处理 |
|---|---|---|
| 207 个小区 | 217 个小区 | 已更正为全量 217 |
| 正好 7 天、每小区 2,016 点 | 2,017 点（7 天 + 5 分钟） | 已更正 |
| 理论总观测 417,312 | 实际 437,689，且无缺失 | 已更正 |
| 起止时间、时区待填 | `2024-11-30 23:00:00`–`2024-12-07 23:00:00` | 已按采集日历写明；时区未另行指定 |
| `traffic` 口径待填 | 5 分钟窗口对上行流量求和得到的原始整数计数，未归一化 | 已按业务口径写明 |
| 缺失 / 零值待填 | 无缺失、无零值、无负值 | 已按实测写明 |
| 训练 / 测试划分待填 | 前 6 天训练，其后 289 点测试 | 已写明 |
| 预测长度待填 | 289 步（24 小时 5 分钟） | 已写明 |
| 最终评测指标 | MAE、RMSE、MAPE、Masked MAPE @ 3e5、Masked MAPE @ 1e6 | 五项并列，各评一名冠军 |

仍无法从源文件还原、因而保持“未知”的信息：

- 时区未另行指定；
- 计数单位到 Byte / packet / Mbps 的换算。
